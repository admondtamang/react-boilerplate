import Axios from "axios";
import { addUser } from "./userAction";

export function addUserFromApi() {
  console.log("hello");
  //   return (dispatch) => {
  //     Axios.get("https://jsonplaceholder.typicode.com/users").then((user) => {
  //       dispatch(addUser(user.data[1]));
  //     });
  //   };
}
